# /etc/portage/env/freerdp-ffmpeg-fix.conf
# Fix freerdp 3.14.1 build with FFmpeg 7+ (FF_PROFILE_* renamed to AV_PROFILE_*)
# Remove once freerdp >= 3.15.0 is in Gentoo
CFLAGS="${CFLAGS} -DFF_PROFILE_AAC_MAIN=AV_PROFILE_AAC_MAIN"
