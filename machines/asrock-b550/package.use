# Gentoo package.use - ASRock B550

# Auto grub-mkconfig on make install (kernel updates)
sys-kernel/installkernel grub

# NVIDIA proprietary driver (RTX 5060 Ti GB206 Blackwell)
# Blackwell REQUIRES kernel-open modules — closed-source not supported
x11-drivers/nvidia-drivers kernel-open modules tools
media-libs/mesa -video_cards_nouveau

# NetworkManager — WiFi + BT support (Intel AX200)
net-misc/networkmanager wifi bluetooth

# xfce4-screensaver wayland requirement
gui-libs/libwlembed gtk

# app-containers/podman requires iptables built with nftables. Portage wrote
# this into package.use/shared as autounmask output on this machine; kept here
# instead so deploying the shared file cannot strip it, and so machines without
# podman are not forced into an iptables rebuild.
# required by app-containers/containers-common, app-containers/podman
>=net-firewall/iptables-1.8.13 nftables
